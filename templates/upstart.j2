# {{name}} service
#
description	"{{name}}"

start on (runlevel [2345])
stop on runlevel [!2345]

respawn
expect fork

script 
  cd {{path}}/{{name}}
  exec {{path}}/{{name}}/{{name}} &
end script

emits {{name}}_starting
